import { z } from 'zod';

export const Consumable_modelScalarFieldEnumSchema = z.enum(['consumable_model_id','manufacturer','consumable_name','consumable_type']);

export default Consumable_modelScalarFieldEnumSchema;
