import { z } from 'zod';

export const Device_consumable_compatibilityScalarFieldEnumSchema = z.enum(['device_model_id','consumable_model_id']);

export default Device_consumable_compatibilityScalarFieldEnumSchema;
